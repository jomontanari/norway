Server Startup (Requires node.js to be setup on the machine)
------------------------------------------------------------

Open the terminal and change to the server directory under src.
Type node server.js

The server should now be running under port 8000

The urls avaliable are

http://localhost:8000/books/list
http://localhost:8000/cds/list
http://localhost:8000/dvds/list